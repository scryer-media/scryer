# Crate Boundaries (Design-Only)

No Rust code is implemented yet by design.

This folder reserves crate/module boundaries that will be realized after intentions/specs and
architecture declarations are reviewed.
